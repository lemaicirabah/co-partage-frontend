image: node:16

stages:
  - install
  - build

cache:
  paths:
    - node_modules/
    - .vite/

install_dependencies:
  stage: install
  script:
    - npm ci
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/

build:
  stage: build
  script:
    - npx vue-tsc --noEmit
    - npm run build
  artifacts:
    paths:
      - dist/
    reports:
      junit: report.xml